
//通过C语言编程求解同余方程ax≡b(mod n),列出啊a,b,n。求存在的x的值

#include<stdio.h>
int main(void)
{
	int a,b,n,i;
	printf("a=");
	scanf("%d",&a);
	printf("b=");
	scanf("%d",&b);
	printf("n=");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		if((a*i-b)%n==0)
			break;
		printf("%d ",i);
	}
	return 0;
}
