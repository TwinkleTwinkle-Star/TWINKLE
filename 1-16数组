/*
输入六个数到2行3列的二维数组a中，将二维数组a中的数组元素转置,
即行列互换，存储到3行2列的二维数组b中，输出二维数组b中的数组元素。
*/

#include<stdio.h>
int main(void)
{
	int i, j, a[2][3], b[3][2];
	printf("输入6个整数:");
	for (i=0;i<2;i++)
	{
		for (j=0;j<3; j++)
		{
			scanf("%d",&a[i][j]);
			printf("%d  ",a[i][j]);
		}
		printf("\n");
	}
	printf("转置后输出:\n");
	for (i=0;i<3;i++)
	{
		for(j=0;j<2;j++)
		{
			b[i][j]=a[j][i];
		}
	}
	for(i=0;i<3;i++) 
	{
		for (j=0;j<2;j++)
		{
			printf("%5d",b[i][j]);
		}
		printf("\n");
	}
      return 0;
}
